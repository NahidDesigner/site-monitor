httpx>=0.27,<1.0
# Brotli is what makes httpx able to DECODE `content-encoding: br`.
# Without it, every Brotli response (i.e. most of Cloudflare) arrives as
# binary in response.text and every regex over the page silently matches
# nothing. Not optional: it is a correctness dependency, not a nicety.
brotli>=1.1
PyYAML>=6.0
python-dotenv>=1.0

# Dashboard
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9

# Excel export
openpyxl>=3.1

# MCP server (lets Claude drive the monitor)
mcp>=2.0,<3
